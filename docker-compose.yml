services:
  strava-to-llm:
    image: romanew/all:strava-to-llm
    restart: unless-stopped
    ports:
      - "10004:8000"
    environment:
      STRAVA_CLIENT_ID: ${STRAVA_CLIENT_ID}
      STRAVA_CLIENT_SECRET: ${STRAVA_CLIENT_SECRET}
    volumes:
      - ./data:/app/data     # per-user secrets/tokens/reports + shared cache
